# [特性名称]接入方案

## 环境

- 工程：
- 本机 SDK：根目录、`sdk-pkg.json`、API/版本
- 工程 API：compile / target / compatible
- 应用模型与 module：
- 应用材质状态（原始值 / 规范化有效值，如适用）：
- 目标组件与设备：

## 可行性结论

- 状态：supported / conditional / upgrade_available / unsupported / insufficient_context
- 升级选项（如适用）：
- 证据与限制：

## 路线选择

- `availableRoutes`：
- `recommendedRoute`：
- `selectedRoutes` 及各自理由：
- 用户确认（多路线或含升级时必填）：
- 未选择路线及理由：

## 改动范围

- 配置与依赖：
- 各路线的模块、页面与组件：
- ArkUI 组件生效域（如适用）：
- 生命周期与异常处理：

## 接入前源程序状态基线

- 原组件树或业务代码路径：
- 断点、方向、窗口与设备形态：
- 状态、数据、控制器、事件与生命周期：
- 拒绝、异常、离线或外部服务不可用行为：
- 普通视觉样式：

## 兼容与回退

- 触发条件：旧版本 / 设备不支持 / 未授权 / 特性关闭 / 外部条件不满足
- `fallbackPolicy`：
- 各条件下继续使用的源程序路径：
- 必须保持不变的状态、布局、交互、数据和视觉维度：

## 性能、隐私与安全

- 性能风险：
- 权限、隐私与发布影响：

## 验证计划

- 各路线静态检查：
- 各路线构建：
- 各路线真机与负向场景：
- 接入前基线与回退分支逐项对照：
